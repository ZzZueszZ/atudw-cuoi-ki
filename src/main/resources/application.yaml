spring:
  datasource:
    url: jdbc:mysql://localhost:3306/atudw
    username: root
    password: D0ga00g123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: loc.pakirat.tech@gmail.com
    password: tmazpciuaxzpqkcw
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8
server:
  port: 8083